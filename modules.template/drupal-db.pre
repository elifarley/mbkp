#!/bin/sh

drupal_off() {
  pushd "$SITE_ROOT/community" || exit 1
  php _offline.php || {
    popd
    exit 2
  }
  popd || exit 3
}

drupal_off

db_dump drupal-backup

#

full_if_older_than="6M"
extra=''

